interface:
  display_name: "Unslop Preflight"
  short_description: "Repo-specific patterns and workflows for unslop-preflight"
  default_prompt: "Use the unslop-preflight repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true